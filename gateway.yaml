apiVersoin: networking.k8s.io/v1
kind: Gateway
metadata:
  name: adithi-gateway
  namespace: adithi-ns
spec:
  gatewayClassName: kgateway
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: same