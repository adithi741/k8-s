apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adithi-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: clusterip-service
                port:
                  number: 5656